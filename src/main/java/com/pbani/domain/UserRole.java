package com.pbani.domain;

//An enum is a special class used to define a collection of constants
//way to create a custom data type that can only hold a specific, predefined set of values
//restrict options as opposed to just using string or integer
public enum UserRole {

    ROLE_USER,
    ROLE_ADMIN,
    ROLE_CASHIER,
    ROLE_BRANCH_MANAGER,
    ROLE_STORE_MANAGER
}
